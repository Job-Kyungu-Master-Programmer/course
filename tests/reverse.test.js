const reverse = require('../Utils/for_testing').reverse

test('reverse of Dating ' , () => {
    const result = reverse('a')
    expect(result).toBe('a')
})

test('reverse of React', () => {
    const result = reverse('a')
    expect(result).toBe('a')
})
